# EMS 售前调研工具系统运维手册（当前版本）

## 1. 运行架构

- 前端：React + Vite 静态站点
- 数据与鉴权：Supabase
- AI 报告：Gemini API（可选）

## 2. 运维基线

### 2.1 环境要求

- Node.js 18+
- npm 9+
- 可访问 Supabase

### 2.2 常用命令

```bash
npm install
npm run dev
npm run build
npm run preview
npm run lint
```

## 3. 初始化与升级

### 3.1 新环境初始化

1. 配置 `.env.local`
2. 执行 `supabase-init.sql`
3. 启动项目并验证核心路径

### 3.2 老环境修复

执行 `supabase-repair-existing.sql`，用于修复：

- 字段类型不一致
- 外键缺失
- 历史脏数据

## 4. 发布流程（建议）

1. 本地执行 `npm run lint` 与 `npm run build`
2. 合并到主分支
3. 由 Cloudflare Pages 自动部署
4. 发布后执行回归检查清单

## 5. 监控与巡检

### 5.1 日常巡检项

- Supabase 项目状态
- 关键表读写是否正常
- 登录、调研提交、报告查看是否可用
- 消息与日志是否持续更新

### 5.2 故障定位顺序

1. 浏览器控制台报错
2. Supabase 查询与策略
3. 环境变量配置
4. 第三方 API（Gemini）可用性

## 6. 备份与恢复

- 使用 Supabase 自动备份
- 关键节点手工导出 SQL/CSV
- 恢复后优先验证 `users`、`survey_forms`、`survey_reports` 三表一致性

## 7. 安全要求

1. 严禁在前端暴露高权限密钥
2. RLS 策略需按角色和数据归属最小化
3. 定期轮换密钥并更新部署环境变量
4. 定期审计异常登录、越权访问与关键写操作

## 8. 风险项与改进建议

- 当前前端存在 service role 回退密钥逻辑，建议尽快移除
- AI 报告生成建议迁移服务端，避免密钥暴露
- 建议补充自动化测试与发布前检查脚本
